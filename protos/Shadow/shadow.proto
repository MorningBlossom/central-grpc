syntax = "proto3";

package Shadow;

option go_package = "://github.com";

service Shadow {
  rpc SaySearchAds(RequestInfo) returns (ReplyShadow) {}
  rpc SayHomeAds(RequestInfo) returns (ReplyShadow) {}
}

message RequestInfo {
  string name = 1;
}

message ReplyShadow {
  string message = 1;
}